/**
 * Output formatting styles for compiled CSS.
 */
export enum SassStyleEnum {
    nested = 0,
    expanded = 1,
    compact = 2,
    compressed = 3,
}

/**
 * Inline source comment modes.
 */
export enum SassCommentsEnum {
    none = 0,
    default = 1,
}
