/**
 * Represents the different sections available in the settings tab.
 */
export enum SettingsSection {
    Editor = 'editor',
    Theme = 'theme',
    Extension = 'extension',
    CloudSync = 'cloud-sync',
    Storage = 'storage',
}
